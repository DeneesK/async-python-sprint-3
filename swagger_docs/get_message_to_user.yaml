---
tags:
- Message Handleres
summary: get and store message for a certain user
description: Receives a message from the client intended for one user and serializes it into a MessageModel, then stores it in dict{to_user, msg}
parameters:
- in: body
  name: body
  required: true
  schema:
    type: object
    properties:
      id:
        type: string
      name:
        type: string
      msg:
        type: string
      to_user:
        type: string
responses:
  "200":
    description: OK