---
tags:
- Connect to server
summary: Delete client from the connected list
description: Delete client from the connected list
produces:
  - text/plain
parameters:
- in: body
  name: body
  required: true
  schema:
    type: object
    properties:
      id:
        type: string
      name:
        type: string
responses:
  "200":
    description: OK