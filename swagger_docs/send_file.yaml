---
tags:
- Files Handleres
summary: send file to users
description: Send file to user. It also notes which files have already been sent to users, so that everyone receives the file only once.
parameters:
- in: body
  name: body
  required: true
  schema:
    type: object
    properties:
      id:
        type: string
      name:
        type: string
responses:
  "200":
    description: Return array of bytes