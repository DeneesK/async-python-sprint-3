---
tags:
- Message Handleres
summary: send last unreceived messages 
description: Sends the last unreceived messages to the user. Using the id of the last message stored in the ServerClientModel. It also saves the ID of the last message sent.
parameters:
- in: body
  name: body
  required: true
  schema:
    type: object
    properties:
      id:
        type: string
      name:
        type: string
responses:
  "200":
    description: OK